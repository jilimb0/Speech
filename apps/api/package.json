{
  "name": "@speech/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@tgwrapper/core": "github:jilimb0/TGWrapper#main",
    "@speech/analysis": "workspace:*",
    "@speech/sessions": "workspace:*",
    "@speech/shared": "workspace:*",
    "@speech/speech": "workspace:*",
    "@fastify/cors": "^10.0.2",
    "fastify": "^5.2.1"
  },
  "devDependencies": {}
}
