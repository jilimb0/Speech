{
  "name": "@speech/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/static": "^9.3.0",
    "@sentry/node": "^10.62.0",
    "@speech/analysis": "workspace:*",
    "@speech/sessions": "workspace:*",
    "@speech/shared": "workspace:*",
    "@speech/speech": "workspace:*",
    "@tgwrapper/core": "^0.18.0",
    "fastify": "^5.2.1"
  }
}
